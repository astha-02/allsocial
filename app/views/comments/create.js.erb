$('#comments-list-<%= @post.id %>').append('<%= escape_javascript render "comment", com: @comment%>');
$('#comments-container-<%= @post.id %> textarea').val('');
noty({
	text: 'Commented successfully',
	type: 'information',
	timeout: 2000
});